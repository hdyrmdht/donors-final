import 'package:flutter/material.dart';

Widget appBarPhoneNumber() => AppBar(
      centerTitle: true,
      title: const Text(
        "Enter Your Phone Number",
        style: TextStyle(
          color: Colors.green,
          fontSize: 18,
        ),
      ),
    );
